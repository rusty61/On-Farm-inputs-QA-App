# --- Supabase ---
SUPABASE_URL=https://<project>.supabase.co
# Use ANON on frontend; SERVICE_ROLE_KEY on backend only (never ship to browser!)
SUPABASE_ANON_KEY=eyJhbGciOi...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...
SUPABASE_BUCKET=infield-spray-records
SUPABASE_SCHEMA=public

# --- App settings ---
APP_ENV=dev
LOG_LEVEL=info
# If you generate absolute URLs (e.g., for QR codes)
PUBLIC_BASE_URL=https://<your-render-service>.onrender.com

# --- PDF ---
PDF_ENGINE=weasyprint

# --- Render (injected automatically in runtime) ---
# PORT is provided by Render for web services. Keep here for local parity.
PORT=8000
